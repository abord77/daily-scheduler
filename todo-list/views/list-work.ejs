<%- include("header") -%>

  <div class="box" id="heading">
    <h1> <%= listTitle %> </h1>
  </div>

  <div class="box">
    <% if (locals.tasksPersonal) { %>
      <% for (let i = 0; i < tasksWork.length; i++) { %>
        <div class="item">
          <input type="checkbox">
          <p><%=  tasksWork[i]  %></p>
        </div>
      <% } %>
    <% } %>

      <form class="item" action="/work" method="POST">
        <input type="text" name="taskName" placeholder="Task..." />
        <button type="submit" name="list">+</button>
      </form>
  </div>

<%- include("footer") -%>
